Place screenshots here:

- `github-repo.png` — screenshot of the GitHub repository page (see first attachment).
- `mongodb-compass.png` — screenshot of the MongoDB Compass view showing the `wedding.gallery` collection (see second attachment).

You can replace these placeholder images with the actual outputs you captured from your environment.